{
  "name": "skuprofit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "check": "node --check server.mjs && node --check app.js && node --check auth.js && node --check dashboard.js && node --check admin.js && node --check contact.js"
  },
  "engines": {
    "node": ">=20"
  }
}
